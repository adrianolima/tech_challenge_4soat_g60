apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: totem-postgres
  namespace: totem
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
