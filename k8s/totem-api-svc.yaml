apiVersion: v1
kind: Service
metadata:
  labels:
    run: totem-api
  name: totem-api
  namespace: totem
spec:
  type: NodePort
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3000
      nodePort: 31000
  selector:
    app: totem-api
